# Local, git-ignored workspace dev state (plugin-self-consume): initiative
# artefacts, memory/journal, events.jsonl. Never tracked or shipped to consumers.
.workspace/
.idea

# Python bytecode (generated when hooks are executed; never shipped)
__pycache__/
*.pyc
